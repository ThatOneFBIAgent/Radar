# ╔══════════════════════════════════════════════════════════╗
# ║                RADAR — Configuration                    ║
# ╠══════════════════════════════════════════════════════════╣
# ║  Edit this file to customize application behavior.      ║
# ║  Restart the application after changes (except themes). ║
# ╚══════════════════════════════════════════════════════════╝

[general]
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
log_level = "INFO"

# Unit system: "metric" or "imperial"
units = "metric"

[earthquake]
# USGS GeoJSON feed URL
# Options:
#   all_hour, all_day, all_week, all_month
#   1.0_hour, 1.0_day, 1.0_week, 1.0_month
#   2.5_hour, 2.5_day, 2.5_week, 2.5_month
#   4.5_hour, 4.5_day, 4.5_week, 4.5_month
#   significant_hour, significant_day, significant_week, significant_month
feed = "all_week"

# Polling interval in seconds (minimum 30)
poll_interval = 60

# Magnitude threshold for highlighting (events >= this flash)
highlight_threshold = 4.5

# Maximum events to display in the list
max_display = 100

[weather]
# Location coordinates (default: Houston, TX)
latitude = 19.4326
longitude = -99.1332
location_name = "CDMX, Mexico"

# Polling interval in seconds (minimum 60)
poll_interval = 120

# Show forecast panel (requires zoom into region)
show_forecast = false

[ui]
# Active/default theme filename when opening (without .json extension)
theme = "obsidian"

# Font size in pixels
font_size = 15

# Window dimensions (0 = auto)
window_width = 1400
window_height = 900

# Start maximized (this ignores window_width and window_height if true)
start_maximized = true

# Enable animations
animations = true

# Animation speed multiplier (0.5 = slow, 2.0 = fast)
animation_speed = 1.0

[audio]
# Enable or disable UI sound effects
enabled = true

# Master volume (0.0 = mute, 1.0 = full)
volume = 0.7

# Radius in km from weather station — if a quake is within this range,
# the "felt" alert sound plays and a warning indicator blinks.
felt_radius_km = 300.0

# How long (seconds) the "FELT" warning blinks on the scanner header.
felt_warning_duration_s = 240

[debug]
# :3
mock_feed_file = ""
